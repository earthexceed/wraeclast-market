<div local-class="sections">
  <section local-class="section">
    <h1 local-class="section-title">
      {{t "page.about.whats-new.title"}}
    </h1>

    <p local-class="section-description">
      {{t "page.about.whats-new.description"}}
    </p>

    <Button
      @theme="gold"
      @icon="info-circle"
      @onClick={{this.openChangelog}}
      @label={{t "page.about.whats-new.button"}}
    />
  </section>

  <section local-class="section">
    <h1 local-class="section-title">
      {{t "page.about.enhancers.title"}}
    </h1>

    {{#each this.enhancers as |enhancer|}}
      <Form::Checkbox
        local-class="enhancer-checkbox"
        @value={{enhancer.isEnabled}}
        @label={{t enhancer.translationKey}}
        @onChange={{fn this.toggleDisabledEnhancerSlug enhancer.slug}}
      />
    {{/each}}
  </section>

  <section local-class="section">
    <h1 local-class="section-title">
      {{t "page.about.github.title"}}
    </h1>

    <p local-class="section-description">
      {{t "page.about.github.description"}}
    </p>

    <Button
      @href={{this.githubUrl}}
      @theme="blue"
      @icon="github"
      @label={{t "page.about.github.button"}}
    />
  </section>

  <section local-class="section">
    <h1 local-class="section-title">
      {{t "page.about.credits.title"}}
    </h1>

    <p local-class="section-description">
      {{t "page.about.credits.description" htmlSafe=true}}
    </p>
  </section>
</div>

<p local-class="disclaimer">
  {{t "page.about.disclaimer"}}
</p>

<p local-class="version">
  {{this.appVersion}}
</p>
